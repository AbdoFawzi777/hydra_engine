name: hydra_engine
description: A Flutter package for network login cracking (SSH, FTP, HTTP)
version: 1.0.0
homepage: https://github.com/AbdoFawzi777/redops-hub/tree/main/packages/hydra_engine

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
